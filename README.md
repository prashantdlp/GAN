# GAN

This repo has code for gan trained on mnist digit dataset (see gan (2).ipynb ) and also an additional xor.ipynb using neural networks 😁
also, don't look inside gan.ipynb as it got corrupted !!!
